<resources>
    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorOnPrimary">@color/colorWhite</item>
        <item name="colorSecondary">@color/colorSecondary</item>
        <item name="android:textColorSecondary">@color/colorBlack</item>
        <item name="materialAlertDialogTheme">@style/AlertDialogTheme</item>
        <item name="materialThemeOverlay">@style/FloatingActionButtonThemeOverlay</item>
    </style>

    <!-- FloatingActionButton theme. -->
    <style name="FloatingActionButtonTheme" parent="@style/Widget.MaterialComponents.FloatingActionButton">
        <item name="materialThemeOverlay">@style/FloatingActionButtonThemeOverlay</item>
    </style>

    <!-- FloatingActionButton theme overlay. -->
    <style name="FloatingActionButtonThemeOverlay">
        <item name="colorOnSecondary">@color/colorWhite</item>
    </style>

    <!--Alert dialog theme placeholder. -->
    <style name="AlertDialogTheme" parent="Theme.MaterialComponents.Light.Dialog.Alert" />

    <!--Action bar theme. -->
    <style name="ToolbarTheme" parent="ThemeOverlay.MaterialComponents.Toolbar.Primary">
        <item name="colorOnPrimary">@color/colorWhite</item>
    </style>

</resources>